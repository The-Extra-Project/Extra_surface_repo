FROM python:slim-bookworm

WORKDIR /app/api/

ARG PUBLIC_HOSTING
COPY requirements.txt /app/api/requirements.txt 
RUN pip install -r requirements.txt
COPY . /app/api/ 
EXPOSE 8000

CMD  uvicorn server:fastapi --host ${BACKEND_IP} --port  8000
